<Window x:Class="ProcessManagerUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProcessManagerUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">

            <Button Content="🔄 Listeyi Yenile" Click="Button_Click" Width="120" Height="30" Margin="0,0,10,0"/>

            <TextBlock Text="Ara:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="Bold"/>
            <TextBox x:Name="txtSearch" Width="200" Height="30" VerticalContentAlignment="Center" Margin="0,0,20,0" TextChanged="TxtSearch_TextChanged"/>

            <TextBlock x:Name="lblStatus" Text="Hazır" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>

        </StackPanel>

        <DataGrid x:Name="gridProcesses" Grid.Row="1" Margin="10" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="İşlem Adı" Binding="{Binding ProcessName}" Width="*"/>
                <DataGridTextColumn Header="PID (ID)" Binding="{Binding ProcessID}" Width="100"/>
                <DataGridTextColumn Header="RAM (MB)" Binding="{Binding RamUsage}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="💀 Seçili Görevi Sonlandır" Click="BtnKill_Click" Background="DarkRed" Foreground="White" FontWeight="Bold" Height="40" Margin="10"/>
    </Grid>
</Window>
